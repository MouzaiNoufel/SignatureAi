# Core ML stack — Python 3.12 compatible.
# TF 2.17+ requires protobuf >=5.28; the pin below avoids the common
# "cannot import name 'runtime_version' from 'google.protobuf'" error.
tensorflow>=2.16,<2.22
protobuf>=5.28,<6
numpy>=1.26
opencv-python>=4.9.0
scikit-learn>=1.4.0
matplotlib>=3.8.0
Pillow>=10.2.0
seaborn>=0.13.0

# Web app
Flask>=3.0.0
Werkzeug>=3.0.0

# Utilities
tqdm>=4.66.0
